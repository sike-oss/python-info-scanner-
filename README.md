# python-info-scanner
This is a python base app where in main login page user can 
fill the basic information that after the information is 
filled successfully they can click on generate qr code 
button then a Qr bar code will be generated user can download 
that qr and anyone can scan that qr code eith default 
scanner of thier phone and when they will scan it the information
which was filled by the user will be displayed on 
the screen